package mockdata

object MockFiles {
  val projectRoot = "server/src/main/scala/mockdata/"
  val jobsPostJsonLoc = projectRoot + "job-posts-mock.json"
  val messagesJsonLoc = projectRoot + "messages-mock.json"
}
